{% extends 'base.html.twig' %}
{% block title %}{{ 'register.title'|trans }}{% endblock %}

{% block stylesheets %}
    <link href="{{ asset('css/register.css') }}" rel="stylesheet">
{% endblock %}

{% block after_nav %}
<div class="view" style="background-image: url('http://mdbootstrap.com/img/Photos/Others/images/91.jpg'); background-repeat: no-repeat; background-size: cover; background-position: center center;">
    <!-- Mask & flexbox options-->
    <div class="mask rgba-gradient d-flex justify-content-center align-items-center">
      <!-- Content -->
      <div class="container">
        <!--Grid row-->
        <div class="row mt-5">
          <!--Grid column-->
          <div class="col-md-6 mb-5 mt-md-0 mt-5 white-text text-center text-md-left">
            <h1 class="h1-responsive font-weight-bold wow fadeInLeft" data-wow-delay="0.3s">Sign up right now! </h1>
            <hr class="hr-light wow fadeInLeft" data-wow-delay="0.3s">
            <h6 class="mb-3 wow fadeInLeft" data-wow-delay="0.3s">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Rem repellendus quasi fuga
            nesciunt dolorum nulla magnam veniam sapiente, fugiat! Commodi sequi non animi ea
            dolor molestiae, quisquam iste, maiores. Nulla.</h6>
            <a class="btn btn-outline-white wow fadeInLeft" data-wow-delay="0.3s">Learn more</a>
          </div>
          <!--Grid column-->
          <!--Grid column-->
          <div class="col-md-6 col-xl-5 mb-4">
            <!--Form-->
            <div class="card wow fadeInRight" data-wow-delay="0.3s">
              <div class="card-body">
                <!--Header-->
                <div class="text-center">
                  <h3 class="white-text">
                    <i class="fa fa-user-plus white-text"></i> {{ 'register.h1'|trans }}</h3>
                  <hr class="hr-light">
                </div>
                <!--Body-->
                {{ form_start(form) }}
                    {% if not form.vars.valid %}
                        {% for children in form.children %}
                            {% if not children.vars.valid %}
                                <div class="alert alert-danger">
                                    {% if children.first is defined %}
                                        {{ form_errors(children.first) }}
                                    {% else %}
                                        {{ form_errors(children) }}
                                    {% endif %}
                                </div>
                            {% endif %}
                        {% endfor %}
                    {% endif %}
                    <div class="md-form">
                        <i class="fa fa-user prefix white-text active"></i>
                        {{ form_widget(form.username, {'id': 'username', 'attr': {'class': 'white-text form-control'}}) }}
                        <label class="active" for="username">{{ 'form.username'|trans }}</label>
                    </div>
                    <div class="md-form">
                        <i class="fa fa-envelope prefix white-text active"></i>
                        {{ form_widget(form.email, {'id': 'email', 'attr': {'class': 'white-text form-control'}}) }}
                        <label class="active" for="email">{{ 'form.email'|trans }}</label>
                    </div>
                    <div class="md-form">
                        <i class="fa fa-lock prefix white-text active"></i>
                        {{ form_widget(form.plainPassword.first, {'id': 'first_password', 'attr': {'class': 'white-text form-control'}}) }}
                        <label for="first_password">{{ 'form.password'|trans }}</label>
                    </div>
                    <div class="md-form">
                        <i class="fa fa-lock prefix white-text active"></i>
                        {{ form_widget(form.plainPassword.second, {'id': 'second_password', 'attr': {'class': 'white-text form-control'}}) }}
                        <label for="second_password">{{ 'form.confirm_password'|trans }}</label>
                    </div>
                    <div class="text-center mt-4">
                        <button class="btn aqua-gradient btn-round">{{ 'form.do_signup'|trans }}</button>
                        <hr class="hr-light mb-3 mt-4">
                        <div class="inline-ul text-center d-flex justify-content-center">
                            <a class="p-2 m-2 tw-ic">
                                <i class="fab fa-twitter white-text"></i>
                            </a>
                            <a class="p-2 m-2 li-ic">
                                <i class="fab fa-linkedin-in white-text"></i>
                            </a>
                            <a class="p-2 m-2 ins-ic">
                                <i class="fab fa-instagram white-text"></i>
                            </a>
                        </div>
                    </div>
                {{ form_end(form) }}
              </div>
            </div>
            <!--/.Form-->
          </div>
          <!--Grid column-->
        </div>
        <!--Grid row-->
      </div>
      <!-- Content -->
    </div>
    <!-- Mask & flexbox options-->
  </div>
{% endblock %}

{% block main %}
<!-- Content bg area start -->
{# <div class="contact-bg overview-bgi">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <!-- Form content box start -->
                <div class="form-content-box">
                    <!-- logo -->
                        <a href="{{ path('index') }}" class="logo">
                            <span class="last">{{ website.name }}</span>
                        </a>
                        <!-- details -->
                        <div class="details">
                            <h3>{{ 'user.create_account'|trans }}</h3>
                            <!-- Form start-->
                            {{ form_start(form) }}
                                {% if not form.vars.valid %}
                                    {% for children in form.children %}
                                        {% if not children.vars.valid %}
                                            <div class="alert alert-danger">
                                                {% if children.first is defined %}
                                                    {{ form_errors(children.first) }}
                                                {% else %}
                                                    {{ form_errors(children) }}
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                    {% endfor %}
                                {% endif %}
                                <div class="form-group"></div>
                                <div class="form-group">
                                {{ form_widget(form.username, {'attr': {'placeholder': 'form.username', 'class': 'input-text'}}) }}
                                </div>
                                <div class="form-group">
                                {{ form_widget(form.email, {'attr': {'placeholder': 'form.email', 'class': 'input-text'}}) }}
                                </div>
                                <div class="form-group">
                                    {{ form_widget(form.plainPassword.first, {'attr': {'placeholder': 'form.password', 'class': 'input-text'}}) }}
                                </div>
                                <div class="form-group">
                                {{ form_widget(form.plainPassword.second, {'attr': {'placeholder': 'form.password_confirmation', 'class': 'input-text'}}) }}
                                </div>
                                <div class="mb-0">
                                    <button type="submit" class="btn-md btn-theme btn-lg btn-block">{{ 'user.sign_up'|trans }}</button>
                                </div>
                            {{ form_end(form) }}
                            <!-- Form end-->
                        </div>
                        <!-- Footer -->
                        <div class="footer">
                        <a class="pull-left" href="{{ path('index') }}"><span>Retourner &agrave; l'accueil</span></a>
                        <span>
                                {{ 'user.already_member'|trans({'%link%': path('fos_user_security_login')})|raw }}
                            </span>
                        </div>
                </div>
                <!-- Form content box end -->
            </div>
        </div>
    </div>
</div> #}

{% endblock %}

