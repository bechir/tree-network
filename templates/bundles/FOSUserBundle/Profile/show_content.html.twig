{% trans_default_domain 'FOSUserBundle' %}

<div class="contact-3 content-area-6">
    <div class="container">
        <div class="main-title mb-60">
            <p class="lead">{{ 'profile.show.username'|trans }}: <strong>{{ user.username }}</strong></p>
            <p class="lead">{{ 'profile.show.email'|trans }}: <strong>{{ user.email }}</strong></p>
        </div>
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="send-btn mb-0  text-center">
                <a href="{{ path('fos_user_profile_edit') }}" class="btn-md btn-theme"><i class="fa fa-edit fa-lg"></i> Modifier mon profil</a>
            </div>
        </div>
    </div>
</div>
